/* 32) Construir um algoritmo que calcule a média aritmética dos valores 
de um vetor de inteiros. */ 

let vector = [1, 3, 8, 10, 12, 89]
let media
let result

function mediaCalc (vector[i]) {
  for (i = 0; i < vector.length; i++) {
    media.push(vector[i])
  }
  media / (i + 1) = result 
  console.log(result)
}
